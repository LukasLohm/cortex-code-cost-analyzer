# Cortex Code Cost Analyzer - Snowflake Deployment
#
# Deploy with: snow streamlit deploy --replace
#
# IMPORTANT: Before deploying, ensure you have:
# 1. ACCOUNTADMIN role or access to SNOWFLAKE.ACCOUNT_USAGE schema
# 2. A warehouse configured (update query_warehouse below)

definition_version: 2
entities:
  cortex_code_cost_analyzer:
    type: streamlit
    identifier:
      name: CORTEX_CODE_COST_ANALYZER
      database: CORTEX_CODE_ANALYSIS
      schema: APP
    query_warehouse: COMPUTE_WH
    main_file: streamlit_app.py
    artifacts:
      - streamlit_app.py
      - config.py
      - data.py
      - render.py
      - utils.py
